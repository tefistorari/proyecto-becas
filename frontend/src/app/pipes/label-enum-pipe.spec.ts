import { LabelEnumPipe } from './label-enum-pipe';

describe('LabelEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
